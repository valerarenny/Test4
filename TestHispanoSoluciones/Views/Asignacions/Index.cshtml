@model IEnumerable<TestHispanoSoluciones.Models.Asignacion>

@{
    ViewBag.Title = "Asignaciones";
}

<h2>Asignaciones</h2>

<p>
    @Html.ActionLink("Crear Asignacion", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Carreras
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Carrera.Carrera1)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Id_Asignacion }, new { @class = "btn btn-default" }) 
            @Html.ActionLink("Detalles", "Details", new { id=item.Id_Asignacion }, new { @class = "btn btn-primary" }) 
            @Html.ActionLink("Borrar", "Delete", new { id=item.Id_Asignacion }, new { @class = "btn btn-danger"})
        </td>
    </tr>
}

</table>
