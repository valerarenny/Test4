@model IEnumerable<TestHispanoSoluciones.Models.Carrera>

@{
    ViewBag.Title = "Carreras";
}

<h2>Carreras</h2>

<p>
    @Html.ActionLink("Crear Carrera", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Carrera1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Carrera1)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id = item.Id_Carrera }, new { @class = "btn btn-default" })
            @Html.ActionLink("Detalles", "Details", new { id = item.Id_Carrera }, new { @class = "btn btn-primary" })
            
        </td>
    </tr>
}

</table>
